pipeline {
  agent any
  stages {
    stage('Starting...') {
      steps {
        echo 'Starting'
      }
    }
    stage('Testing) {
       steps {
         bat 'java Start'
         bat 'javac Start.java'
       }      
    }
  }
}
